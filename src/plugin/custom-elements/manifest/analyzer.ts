// import { cli } from '@custom-elements-manifest/analyzer/cli.js';
// import { Package } from 'custom-elements-manifest';
//
// export const analyzerCLI = cli as ({
  // argv,
  // cwd,
  // noWrite,
// }: {
  // argv?: string[];
  // cwd?: string;
  // noWrite?: boolean;
// }) => Promise<Package>;
